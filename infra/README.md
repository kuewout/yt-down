# Infrastructure

Place local development infrastructure files here, such as:

- `docker-compose.yml` for PostgreSQL if you later want repo-managed local services
- reverse proxy configuration
- deployment notes
